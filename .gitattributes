# The README table is parsed by nix/check-consistency.py and regenerated by
# tools/update's internal/readme, both of which assume LF. Normalizing on
# checkout keeps a CRLF working tree from ever reaching them.
* text=auto eol=lf
